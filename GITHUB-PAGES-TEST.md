# GitHub Pages 免费测试说明

这个版本不需要 Vercel，也不需要 Firebase Blaze。GitHub Pages 使用 HTTPS，因此可以测试摄像头手势；用户数据、积分和兑换码只保存在当前浏览器中。

## 上传

把项目根目录中的文件全部提交到 GitHub，尤其要包含：

- `.nojekyll`
- `index.html`
- `assets/`
- `docs/`
- 所有皮肤图片目录

在 GitHub 仓库中打开：

1. `Settings`
2. `Pages`
3. `Build and deployment`
4. Source 选择 `Deploy from a branch`
5. Branch 选择你的主分支，目录选择 `/ (root)`
6. 保存并等待部署完成

## 测试

- 测试兑换码：`VELA-TEST-10000`
- 奖励：当前浏览器本地积分 `10000`
- 每个浏览器仅能使用一次；重新部署不会重置浏览器本地数据。
- 如需重新测试，可在浏览器开发者工具中清除此网站的 Local Storage。

## 手势

点击页面中的“查看示意”，再点击“启用魔法手势”。浏览器询问摄像头权限时选择允许。

- 张开手掌：移动星光指针
- 捏合拇指与食指：点击
- 握拳：开始或抽牌
- V 手势：发起解读

如果手势不能启动，请确认：

1. 地址以 `https://` 开头。
2. 浏览器已允许摄像头权限。
3. 网络可以访问 `cdn.jsdelivr.net`，手势识别模型会从该地址加载。
4. 没有其他应用占用摄像头。

## 免费版限制

- GitHub Pages 只有静态网页，不提供可信任的服务器后台。
- 测试兑换码与积分只是本地测试功能，用户可以修改自己的浏览器数据。
- 反馈审核、全局积分、防刷和管理员后台，需要之后接入服务端。

